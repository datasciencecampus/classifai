# build containers for PROD services
steps:
- name: "gcr.io/cloud-builders/gcloud"
  args: ["app", "deploy", "prod_app.yaml"]
- name: "gcr.io/cloud-builders/gcloud"
  args: ["app", "deploy", "prod_flask_app.yaml"]

timeout: '600s'

options:
 logging: CLOUD_LOGGING_ONLY
 env:
  - PROJECT_ID=$_PROJECT_ID
  - _IMAGE=$_IMAGE
