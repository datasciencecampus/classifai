runtime: python311
instance_class: F4_1G
env: standard
service: dev-fastapi    # DEV FastAPI service

entrypoint: gunicorn -b 0.0.0.0:8080 -w 8 -k uvicorn.workers.UvicornWorker fast-api.main:app

automatic_scaling:
  min_instances: 1
  max_instances: 12

env_variables:
  API_TYPE: live
  PROJECT_ID: classifai-sandbox
